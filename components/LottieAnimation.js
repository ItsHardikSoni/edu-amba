'use client';
import Lottie from 'lottie-react';
import animationData from '../public/reading_cat.json';

const LottieAnimation = () => {
  return <Lottie animationData={animationData} />;
};

export default LottieAnimation;
